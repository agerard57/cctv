import { SupportedKeys } from "@/providers/keyState";

export enum UnlockedScreenViews {
  REPLAY_MANAGER = SupportedKeys.F1,
  USER_MANAGER = SupportedKeys.F2,
  CONTROL_CENTER = SupportedKeys.F3,
  SETTINGS = SupportedKeys.F4,
}
